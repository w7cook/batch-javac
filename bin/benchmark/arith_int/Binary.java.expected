package benchmark.arith_int;

public class Binary extends Exp {
    Exp left;
    Exp right;
    
    Binary(Exp l, Exp r) {
        super();
        left = l;
        right = r;
    }
    
    Exp getLeft() {
        return left;
    }
    
    Exp getRight() {
        return right;
    }
}