package benchmark.visitor;

public class Add extends BinOp {
    
    public Add() {
        super();
    }
    
    int apply(int x, int y) {
        return x + y;
    }
    
    static int apply$1005(int x, int y) {
        return x + y;
    }
}